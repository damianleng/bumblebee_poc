services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: poc-bumblebee
    environment:
      DATABASE_URL: ${DATABASE_URL}
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY}
    ports:
      - "8001:8001"
    networks:
      - errekaese_default
    restart: unless-stopped

  frontend:
    build:
      context: ./client
      dockerfile: Dockerfile
    container_name: poc-bumblebee-frontend
    environment:
      VITE_API_URL: ${VITE_API_URL}
    ports:
      - "3000:8080"
    depends_on:
      - backend
    restart: unless-stopped

networks:
  errekaese_default:
    external: true
